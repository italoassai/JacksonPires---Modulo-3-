<p style="color: green"><%= notice %></p>

<h1>Mining types</h1>

<div id="mining_types">
  <% @mining_types.each do |mining_type| %>
    <%= render mining_type %>
    <p>
      <%= link_to t("links.show"), mining_type, class:"btn btn-primary" %>
    </p>
  <% end %>
</div>

<%= link_to t("links.new_mining_type"), new_mining_type_path, class:"btn btn-primary" %>
